let added_comment = $("<%= j(render 'comments/comment', comment: @comment) %>");
<!-- ultra concise version-->
<!-- let added_comment = $("<%= j(render @comment) %>"); -->

$("#<%= dom_id(@comment.photo) %>_new_comment_form").before(added_comment);

<!-- clears out the imput -->
$("#<%= dom_id(@comment.photo) %>_new_comment_form #comment_body").val("");
