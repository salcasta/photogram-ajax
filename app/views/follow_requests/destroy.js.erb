console.log("Follow status changed");

var changeFollowStatus = $("<%= j(render "follow_requests/follow_unfollow", sender: @follow_request.sender, recipient: @follow_request.recipient) %>");

$(".<%=dom_id(@follow_request)%>").replaceWith(changeFollowStatus)
